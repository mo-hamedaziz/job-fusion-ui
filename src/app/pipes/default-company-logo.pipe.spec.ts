import { DefaultCompanyLogoPipe } from './default-company-logo.pipe';

describe('DefaultCompanyLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultCompanyLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
